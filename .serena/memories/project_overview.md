# Project Overview
- Next.js 15 + Tailwind CSS documentation site template sourced from Tailwind Plus.
- Integrates Markdoc for content authoring and custom search via FlexSearch/autocomplete.
- Source lives under `src/` with app router layout, shared components, styles, and Markdoc schemas.
- TypeScript is enabled; project targets modern browsers (per `browserslist`).