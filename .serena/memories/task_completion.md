# Task Completion Checklist
- Ensure source files pass `npm run lint` before handing off changes.
- For UI changes, verify the dev server (`npm run dev`) reflects expected behavior.
- Update documentation/Markdoc content when modifying related features.
- Mention relevant npm scripts or config updates in summaries.